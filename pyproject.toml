[project]
name = "threads-community-manager"
version = "0.1.0"
description = "Local Threads community publishing manager"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "httpx>=0.27.0",
  "keyring>=25.0.0",
  "python-multipart>=0.0.9",
]

[dependency-groups]
dev = ["pytest>=8.0.0", "pytest-asyncio>=0.24.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]

